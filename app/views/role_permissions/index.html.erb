<h1>Lista de Roles / Permissões</h1>

<table>
  <tr>
    <td>Role Name</td>
    <td>Permission Name</td>
  </tr>

<% for role_permission in @role_permissions %>
  <tr>
    <td><%=h role_permission.role.name %></td>
    <td><%=h role_permission.permission.name %></td>
    <td><%= link_to 'Edit', edit_role_permission_path(role_permission) %></td>
    <td><%= link_to 'Destroy', role_permission, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<p class="createNew"><%= link_to 'Criar Nova', new_role_permission_path %></p>
<p class="pageBack"><%= link_to 'Voltar ao Painel de Controle', control_panels_path %></p>

